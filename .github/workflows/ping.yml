name: Ping Backend

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Server
        run: curl https://desertplanners-backend.onrender.com/health